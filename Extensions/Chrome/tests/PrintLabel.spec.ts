/*
 * Filename: PrintLabel.spec.ts
 * Author: Kevin Davis
 * Date: Dec 21, 2018
 *
 * Description
 * Tests for our print label functions
 */
// ==================================================================

//// <reference path="../src/types/healthFair.d.ts" />
//// <reference path="../src/types/patientRecord.d.ts" />

//import * as PrintLabel from "../src/PrintLabel"
//import * as assert from 'assert'

//describe('convertArmToHealthFairName', () => {
	//it('should return the appropriate health fair IDs', () => {
		//// TEST 1
		//let input: number = 1;
		//let expected: Healthfair = "LHHF";
		//let output: Healthfair = PrintLabel.convertArmToHealthFairName(input);
		//assert.equal(output, expected);

		//input = 2;
		//expected = "HHF";
		//output = PrintLabel.convertArmToHealthFairName(input);
		//assert.equal(output, expected);

	//});
//});
